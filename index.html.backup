<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>淇粰鏀剧疆 v0.2</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --gold: #FFD700;
            --blue: #4A90E2;
            --purple: #9B59B6;
            --red: #E74C3C;
            --green: #2ECC71;
            --cyan: #00CED1;
            --orange: #FF8C00;
            --bg-dark: #1a1a2e;
            --bg-card: #16213e;
            --bg-lighter: #1e2a4a;
            --text-primary: #eee;
            --text-secondary: #aaa;
        }

        body {
            font-family: 'Microsoft YaHei', '寰蒋闆呴粦', sans-serif;
            background: linear-gradient(135deg, var(--bg-dark) 0%, #0f0f23 100%);
            color: var(--text-primary);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .game-container {
            max-width: 520px;
            width: 100%;
            background: var(--bg-card);
            border-radius: 16px;
            padding: 24px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }

        /* 椤堕儴鏍囩椤?*/
        .tabs {
            display: flex;
            gap: 8px;
            margin-bottom: 20px;
            background: rgba(255, 255, 255, 0.05);
            padding: 4px;
            border-radius: 12px;
        }

        .tab {
            flex: 1;
            padding: 10px;
            text-align: center;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 14px;
            font-weight: 500;
        }

        .tab:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .tab.active {
            background: linear-gradient(45deg, var(--blue), var(--purple));
            color: white;
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        .title {
            font-size: 28px;
            background: linear-gradient(45deg, var(--gold), #FFA500);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 8px;
        }

        .realm-badge {
            display: inline-block;
            padding: 6px 16px;
            background: linear-gradient(45deg, var(--purple), var(--blue));
            border-radius: 20px;
            font-size: 14px;
            font-weight: bold;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            margin-bottom: 20px;
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 14px;
            text-align: center;
        }

        .stat-label {
            font-size: 12px;
            color: var(--text-secondary);
            margin-bottom: 6px;
        }

        .stat-value {
            font-size: 22px;
            font-weight: bold;
        }

        .stat-value.gold { color: var(--gold); }
        .stat-value.blue { color: var(--blue); }
        .stat-value.green { color: var(--green); }
        .stat-value.purple { color: var(--purple); }

        .progress-section {
            margin-bottom: 20px;
        }

        .progress-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            font-size: 14px;
        }

        .progress-bar {
            height: 24px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            overflow: hidden;
            position: relative;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--blue), var(--purple));
            transition: width 0.3s ease;
            border-radius: 12px;
        }

        .progress-text {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 12px;
            font-weight: bold;
        }

        .action-buttons {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        .btn {
            flex: 1;
            padding: 14px;
            border: none;
            border-radius: 12px;
            font-size: 15px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-cultivate {
            background: linear-gradient(45deg, var(--blue), var(--purple));
            color: white;
        }

        .btn-cultivate:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(74, 144, 226, 0.3);
        }

        .btn-breakthrough {
            background: linear-gradient(45deg, var(--gold), #FFA500);
            color: #333;
        }

        .btn-breakthrough:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(255, 215, 0, 0.3);
        }

        .btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .info-section {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 12px;
            padding: 14px;
            margin-bottom: 14px;
        }

        .info-title {
            font-size: 14px;
            color: var(--text-secondary);
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 8px;
            font-size: 13px;
        }

        .info-item {
            display: flex;
            justify-content: space-between;
        }

        .info-item span:first-child {
            color: var(--text-secondary);
        }

        .auto-cultivate-toggle {
            display: flex;
            align-items: center;
            gap: 8px;
            justify-content: center;
            margin-bottom: 16px;
            font-size: 14px;
        }

        .toggle-switch {
            width: 48px;
            height: 24px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            position: relative;
            cursor: pointer;
            transition: background 0.3s;
        }

        .toggle-switch.active {
            background: var(--blue);
        }

        .toggle-switch::after {
            content: '';
            position: absolute;
            width: 20px;
            height: 20px;
            background: white;
            border-radius: 50%;
            top: 2px;
            left: 2px;
            transition: transform 0.3s;
        }

        .toggle-switch.active::after {
            transform: translateX(24px);
        }

        .message {
            text-align: center;
            padding: 12px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 8px;
            font-size: 14px;
            color: var(--text-secondary);
            margin-top: 14px;
        }

        .message.success { color: var(--green); }
        .message.fail { color: var(--red); }

        /* 鍔熸硶绯荤粺 */
        .skills-section {
            margin-top: 14px;
        }

        .skill-card {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 12px;
            margin-bottom: 10px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .skill-card:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: translateY(-2px);
        }

        .skill-card.equipped {
            border-color: var(--purple);
            background: rgba(155, 89, 182, 0.1);
        }

        .skill-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 6px;
        }

        .skill-name {
            font-weight: bold;
            font-size: 15px;
        }

        .skill-type {
            font-size: 12px;
            padding: 2px 8px;
            border-radius: 10px;
            background: var(--purple);
        }

        .skill-effect {
            font-size: 13px;
            color: var(--green);
            margin-bottom: 4px;
        }

        .skill-desc {
            font-size: 12px;
            color: var(--text-secondary);
        }

        .skill-cost {
            font-size: 12px;
            color: var(--gold);
            margin-top: 6px;
        }

        /* 涓硅嵂绯荤粺 */
        .pills-section {
            margin-top: 14px;
        }

        .pill-card {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 12px;
            margin-bottom: 10px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .pill-card:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: translateY(-2px);
        }

        .pill-card:active {
            transform: scale(0.98);
        }

        .pill-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 6px;
        }

        .pill-name {
            font-weight: bold;
            font-size: 15px;
        }

        .pill-count {
            font-size: 14px;
            color: var(--cyan);
        }

        .pill-effect {
            font-size: 13px;
            color: var(--green);
            margin-bottom: 4px;
        }

        .pill-desc {
            font-size: 12px;
            color: var(--text-secondary);
        }

        /* 鑳屽寘绯荤粺 */
        .inventory-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 8px;
            margin-top: 14px;
        }

        .inventory-slot {
            aspect-ratio: 1;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
        }

        .inventory-slot:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .inventory-slot.empty {
            opacity: 0.5;
        }

        .inventory-slot .icon {
            font-size: 20px;
            margin-bottom: 4px;
        }

        .inventory-slot .count {
            position: absolute;
            bottom: 4px;
            right: 4px;
            font-size: 10px;
            background: rgba(0, 0, 0, 0.6);
            padding: 1px 4px;
            border-radius: 4px;
        }

        .floating-text {
            position: fixed;
            pointer-events: none;
            font-size: 16px;
            font-weight: bold;
            animation: floatUp 1s ease-out forwards;
            z-index: 1000;
        }

        @keyframes floatUp {
            0% {
                opacity: 1;
                transform: translateY(0);
            }
            100% {
                opacity: 0;
                transform: translateY(-50px);
            }
        }

        /* 鍟嗗簵 */
        .shop-section {
            margin-top: 14px;
        }

        .shop-item {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 12px;
            margin-bottom: 10px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .shop-item:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: translateY(-2px);
        }

        .shop-item-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 6px;
        }

        .shop-item-name {
            font-weight: bold;
            font-size: 15px;
        }

        .shop-item-price {
            font-size: 14px;
            color: var(--gold);
        }

        .shop-item-desc {
            font-size: 12px;
            color: var(--text-secondary);
        }
    </style>
</head>
<body>
    <div class="game-container">
        <!-- 鏍囩椤?-->
        <div class="tabs">
            <div class="tab active" onclick="switchTab('main')">淇偧</div>
            <div class="tab" onclick="switchTab('skills')">鍔熸硶</div>
            <div class="tab" onclick="switchTab('pills')">涓硅嵂</div>
            <div class="tab" onclick="switchTab('shop')">鍟嗗簵</div>
        </div>

        <!-- 涓荤晫闈?-->
        <div id="main-tab" class="tab-content active">
            <div class="header">
                <h1 class="title">淇粰鏀剧疆</h1>
                <div class="realm-badge" id="realmBadge">缁冩皵鏈?/div>
            </div>

            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-label">淇负</div>
                    <div class="stat-value gold" id="cultivationValue">0</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">鐏垫皵</div>
                    <div class="stat-value blue" id="spiritValue">100</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">璧勮川</div>
                    <div class="stat-value green" id="talentValue">1.0</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">鎮熸€?/div>
                    <div class="stat-value purple" id="comprehensionValue">1.0</div>
                </div>
            </div>

            <div class="progress-section">
                <div class="progress-header">
                    <span>淇负杩涘害</span>
                    <span id="progressText">0 / 1,000</span>
                </div>
                <div class="progress-bar">
                    <div class="progress-fill" id="progressFill" style="width: 0%"></div>
                    <div class="progress-text" id="progressPercent">0%</div>
                </div>
            </div>

            <div class="action-buttons">
                <button class="btn btn-breakthrough" id="breakthroughBtn" onclick="attemptBreakthrough()" disabled>
                    绐佺牬
                </button>
            </div>

            <div class="info-section">
                <div class="info-title">
                    <span>淇偧淇℃伅</span>
                </div>
                <div class="info-grid">
                    <div class="info-item">
                        <span>淇偧閫熷害:</span>
                        <span id="cultivationSpeed">10/绉?/span>
                    </div>
                    <div class="info-item">
                        <span>鐏垫皵鎭㈠:</span>
                        <span id="spiritRecovery">5/绉?/span>
                    </div>
                    <div class="info-item">
                        <span>鐏电煶:</span>
                        <span id="spiritStones">0</span>
                    </div>
                    <div class="info-item">
                        <span>閬撳績:</span>
                        <span id="daoHeart">+0%</span>
                    </div>
                </div>
            </div>

            <div class="message" id="messageBox">
                淇偧涓?..鐏垫皵鑷姩鎭㈠锛屼慨涓鸿嚜鍔ㄥ闀?            </div>
        </div>

        <!-- 鍔熸硶鐣岄潰 -->
        <div id="skills-tab" class="tab-content">
            <div class="header">
                <h2 class="title" style="font-size: 24px;">鍔熸硶闃?/h2>
                <p style="font-size: 14px; color: var(--text-secondary);">褰撳墠妲戒綅: <span id="skillSlots">0/3</span></p>
            </div>

            <div class="skills-section" id="skillsList">
                <!-- 鍔熸硶鍒楄〃 -->
            </div>
        </div>

        <!-- 涓硅嵂鐣岄潰 -->
        <div id="pills-tab" class="tab-content">
            <div class="header">
                <h2 class="title" style="font-size: 24px;">涓硅嵂闃?/h2>
                <p style="font-size: 14px; color: var(--text-secondary);">涓硅嵂鍙互涓存椂鎴栨案涔呮彁鍗囧睘鎬?/p>
            </div>

            <div class="pills-section" id="pillsList">
                <!-- 涓硅嵂鍒楄〃 -->
            </div>
        </div>

        <!-- 鍟嗗簵鐣岄潰 -->
        <div id="shop-tab" class="tab-content">
            <div class="header">
                <h2 class="title" style="font-size: 24px;">鐏电煶鍟嗗簵</h2>
                <p style="font-size: 14px; color: var(--text-secondary);">鐏电煶: <span id="shopSpiritStones">0</span></p>
            </div>

            <div class="shop-section" id="shopList">
                <!-- 鍟嗗簵鍒楄〃 -->
            </div>
        </div>
    </div>

    <script>
        // 娓告垙閰嶇疆
        const REALMS = [
            { id: 1, name: '缁冩皵鏈?, requiredCultivation: 0, speedMultiplier: 1.0, skillSlots: 1 },
            { id: 2, name: '绛戝熀鏈?, requiredCultivation: 1000, speedMultiplier: 1.5, skillSlots: 2 },
            { id: 3, name: '閲戜腹鏈?, requiredCultivation: 5000, speedMultiplier: 2.0, skillSlots: 3 },
            { id: 4, name: '鍏冨┐鏈?, requiredCultivation: 25000, speedMultiplier: 3.0, skillSlots: 4 },
            { id: 5, name: '鍖栫鏈?, requiredCultivation: 100000, speedMultiplier: 5.0, skillSlots: 5 }
        ];

        const SKILLS = [
            { id: 1, name: '鍚愮撼鏈?, type: '琚姩', effect: '淇偧閫熷害+10%', multiplier: 1.1, cost: 100, desc: '鏈€鍩虹鐨勫悙绾充箣娉曪紝寮曞澶╁湴鐏垫皵鍏ヤ綋' },
            { id: 2, name: '寮曟皵璇€', type: '琚姩', effect: '淇偧閫熷害+20%', multiplier: 1.2, cost: 300, desc: '寮曞鐏垫皵鏇村揩铻嶅叆缁忚剦' },
            { id: 3, name: '鑱氱伒鏈?, type: '琚姩', effect: '淇偧閫熷害+30%', multiplier: 1.3, cost: 800, desc: '鍦ㄥ懆鍥村舰鎴愬皬鍨嬭仛鐏甸樀' },
            { id: 4, name: '鐏靛彴娓呮槑', type: '琚姩', effect: '鎮熸€?0.2', comprehensionBonus: 0.2, cost: 500, desc: '淇濇寔鐏靛彴娓呮槑锛屾彁鍗囨偀鎬? },
            { id: 5, name: '鐐间綋璇€', type: '琚姩', effect: '璧勮川+0.1', talentBonus: 0.1, cost: 600, desc: '閿荤偧鑲夎韩锛屾彁鍗囪祫璐? }
        ];

        const PILLS = [
            { id: 1, name: '鑱氭皵涓?, effect: '淇负+100', cultivationBonus: 100, cost: 50, desc: '钑村惈绾噣鐏垫皵鐨勪腹鑽? },
            { id: 2, name: '鍑濈涓?, effect: '鎮熸€т复鏃?0.5(5鍒嗛挓)', tempComprehension: 0.5, duration: 300, cost: 100, desc: '鐭殏鎻愬崌鎮熸€х殑涓硅嵂' },
            { id: 3, name: '娲楅珦涓?, effect: '璧勮川姘镐箙+0.05', permanentTalent: 0.05, cost: 500, desc: '娲楃簿浼愰珦锛屾彁鍗囧厛澶╄祫璐? },
            { id: 4, name: '绛戝熀涓?, effect: '绐佺牬鎴愬姛鐜?10%', breakthroughBonus: 10, cost: 200, desc: '杈呭姪绛戝熀鐨勭弽璐典腹鑽? },
            { id: 5, name: '鎮熼亾鑼?, effect: '淇负+500', cultivationBonus: 500, cost: 300, desc: '鍝佽尪鎮熼亾锛岀伒姘斿厖鐩? }
        ];

        const SHOP_ITEMS = [
            { id: 1, name: '鑱氭皵涓?, price: 50, type: 'pill', itemId: 1, desc: '淇负+100' },
            { id: 2, name: '鍚愮撼鏈?, price: 100, type: 'skill', itemId: 1, desc: '淇偧閫熷害+10%' },
            { id: 3, name: '鍑濈涓?, price: 100, type: 'pill', itemId: 2, desc: '鎮熸€т复鏃?0.5' },
            { id: 4, name: '绛戝熀涓?, price: 200, type: 'pill', itemId: 4, desc: '绐佺牬鎴愬姛鐜?10%' },
            { id: 5, name: '寮曟皵璇€', price: 300, type: 'skill', itemId: 2, desc: '淇偧閫熷害+20%' }
        ];

        // 鐜╁鏁版嵁
        let playerData = {
            realm: 1,
            cultivation: 0,
            spirit: 100,
            maxSpirit: 100,
            talent: 1.0,
            comprehension: 1.0,
            spiritStones: 0,
            failedBreakthroughs: 0,
            lastOnlineTime: Date.now(),
            autoCultivate: false,
            equippedSkills: [],
            ownedSkills: [],
            inventory: {}, // { pillId: count }
            tempBuffs: {
                comprehension: 0,
                breakthrough: 0
            }
        };

        let gameInterval = null;

        // 鍒濆鍖?        function init() {
            loadGame();
            updateUI();
            renderSkills();
            renderPills();
            renderShop();
            calculateOfflineReward();
            startGameLoop();
        }

        // 鍒囨崲鏍囩椤?        function switchTab(tabName) {
            document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
            document.querySelectorAll('.tab-content').forEach(t => t.classList.remove('active'));
            
            event.target.classList.add('active');
            document.getElementById(`${tabName}-tab`).classList.add('active');
            
            if (tabName === 'skills') renderSkills();
            if (tabName === 'pills') renderPills();
            if (tabName === 'shop') renderShop();
        }

        // 娓告垙涓诲惊鐜?        function startGameLoop() {
            if (gameInterval) clearInterval(gameInterval);
            
            gameInterval = setInterval(() => {
                // 鐏垫皵鎭㈠
                if (playerData.spirit < playerData.maxSpirit) {
                    playerData.spirit = Math.min(
                        playerData.maxSpirit,
                        playerData.spirit + getSpiritRecovery()
                    );
                }

                // 鑷姩淇偧锛堝缁堝紑鍚級
                cultivateTick();

                // 妫€鏌ヤ复鏃禸uff杩囨湡
                checkTempBuffs();

                updateUI();
                saveGame();
            }, 1000);
        }



        function cultivateTick() {
            const gain = getCultivationGain();
            playerData.cultivation += gain;
            playerData.spirit = Math.max(0, playerData.spirit - getSpiritCost());
            
            checkBreakthroughAvailability();
        }

        // 鑾峰彇淇负澧炵泭锛堝寘鍚姛娉曞姞鎴愶級
        function getCultivationGain() {
            const baseRate = 10;
            const realmBonus = REALMS[playerData.realm - 1].speedMultiplier;
            
            // 鍔熸硶鍔犳垚
            let skillMultiplier = 1.0;
            playerData.equippedSkills.forEach(skillId => {
                const skill = SKILLS.find(s => s.id === skillId);
                if (skill && skill.multiplier) {
                    skillMultiplier *= skill.multiplier;
                }
            });
            
            // 璧勮川鍔犳垚锛堝寘鍚案涔呮彁鍗囷級
            let totalTalent = playerData.talent;
            playerData.equippedSkills.forEach(skillId => {
                const skill = SKILLS.find(s => s.id === skillId);
                if (skill && skill.talentBonus) {
                    totalTalent += skill.talentBonus;
                }
            });

            return Math.floor(baseRate * realmBonus * skillMultiplier * totalTalent);
        }

        // 鑾峰彇鎮熸€э紙鍖呭惈鍔熸硶鍜屼复鏃禸uff锛?        function getTotalComprehension() {
            let total = playerData.comprehension;
            
            // 鍔熸硶鍔犳垚
            playerData.equippedSkills.forEach(skillId => {
                const skill = SKILLS.find(s => s.id === skillId);
                if (skill && skill.comprehensionBonus) {
                    total += skill.comprehensionBonus;
                }
            });
            
            // 涓存椂buff
            total += playerData.tempBuffs.comprehension;
            
            return total;
        }

        function getSpiritCost() {
            return playerData.realm;
        }

        function getSpiritRecovery() {
            return 5 + playerData.realm;
        }

        // 绐佺牬
        function attemptBreakthrough() {
            const nextRealm = REALMS[playerData.realm];

            if (!nextRealm || playerData.cultivation < nextRealm.requiredCultivation) {
                showMessage('淇负涓嶈冻锛?, 'fail');
                return;
            }

            const successRate = calculateBreakthroughSuccessRate();
            const isSuccess = Math.random() * 100 < successRate;

            if (isSuccess) {
                playerData.realm++;
                playerData.cultivation = 0;
                playerData.failedBreakthroughs = 0;
                playerData.maxSpirit = 100 + playerData.realm * 10;
                
                showMessage(`鎭枩绐佺牬鑷?${REALMS[playerData.realm - 1].name}锛乣, 'success');
                createFloatingText('绐佺牬鎴愬姛锛?, 'purple');
                
                playerData.spiritStones += playerData.realm * 100;
            } else {
                playerData.cultivation = Math.floor(playerData.cultivation * 0.8);
                playerData.failedBreakthroughs++;
                
                showMessage(`绐佺牬澶辫触锛佹崯澶?0%淇负锛岄亾蹇?5%`, 'fail');
                createFloatingText('绐佺牬澶辫触', 'red');
            }

            updateUI();
            saveGame();
        }

        function calculateBreakthroughSuccessRate() {
            const baseRate = 50;
            const realmPenalty = playerData.realm * 5;
            const comprehensionBonus = getTotalComprehension() * 10;
            const daoHeartBonus = playerData.failedBreakthroughs * 5;
            const pillBonus = playerData.tempBuffs.breakthrough;

            const rate = baseRate - realmPenalty + comprehensionBonus + daoHeartBonus + pillBonus;
            return Math.max(10, Math.min(90, rate));
        }

        // 鍔熸硶绯荤粺
        function renderSkills() {
            const container = document.getElementById('skillsList');
            const maxSlots = REALMS[playerData.realm - 1].skillSlots;
            
            document.getElementById('skillSlots').textContent = 
                `${playerData.equippedSkills.length}/${maxSlots}`;

            container.innerHTML = SKILLS.map(skill => {
                const owned = playerData.ownedSkills.includes(skill.id);
                const equipped = playerData.equippedSkills.includes(skill.id);
                const canEquip = owned && !equipped && playerData.equippedSkills.length < maxSlots;
                
                return `
                    <div class="skill-card ${equipped ? 'equipped' : ''}" 
                         onclick="${owned ? `toggleSkill(${skill.id})` : `buySkill(${skill.id})`}">
                        <div class="skill-header">
                            <span class="skill-name">${skill.name}</span>
                            <span class="skill-type">${skill.type}</span>
                        </div>
                        <div class="skill-effect">${skill.effect}</div>
                        <div class="skill-desc">${skill.desc}</div>
                        ${!owned ? `<div class="skill-cost">璐拱: ${skill.cost} 鐏电煶</div>` : 
                          (equipped ? '<div style="color: var(--green); font-size: 12px; margin-top: 6px;">宸茶澶?/div>' :
                           '<div style="color: var(--text-secondary); font-size: 12px; margin-top: 6px;">鐐瑰嚮瑁呭</div>')}
                    </div>
                `;
            }).join('');
        }

        function buySkill(skillId) {
            const skill = SKILLS.find(s => s.id === skillId);
            if (!skill || playerData.ownedSkills.includes(skillId)) return;
            
            if (playerData.spiritStones < skill.cost) {
                showMessage('鐏电煶涓嶈冻锛?, 'fail');
                return;
            }
            
            playerData.spiritStones -= skill.cost;
            playerData.ownedSkills.push(skillId);
            
            showMessage(`涔犲緱銆?{skill.name}銆嬶紒`, 'success');
            createFloatingText('涔犲緱鍔熸硶锛?, 'purple');
            
            updateUI();
            renderSkills();
            saveGame();
        }

        function toggleSkill(skillId) {
            const maxSlots = REALMS[playerData.realm - 1].skillSlots;
            const index = playerData.equippedSkills.indexOf(skillId);
            
            if (index > -1) {
                playerData.equippedSkills.splice(index, 1);
            } else if (playerData.equippedSkills.length < maxSlots) {
                playerData.equippedSkills.push(skillId);
            }
            
            renderSkills();
            saveGame();
        }

        // 涓硅嵂绯荤粺
        function renderPills() {
            const container = document.getElementById('pillsList');
            
            container.innerHTML = PILLS.map(pill => {
                const count = playerData.inventory[pill.id] || 0;
                
                return `
                    <div class="pill-card ${count === 0 ? 'empty' : ''}" 
                         onclick="${count > 0 ? `usePill(${pill.id})` : ''}">
                        <div class="pill-header">
                            <span class="pill-name">${pill.name}</span>
                            <span class="pill-count">脳${count}</span>
                        </div>
                        <div class="pill-effect">${pill.effect}</div>
                        <div class="pill-desc">${pill.desc}</div>
                    </div>
                `;
            }).join('');
        }

        function usePill(pillId) {
            const pill = PILLS.find(p => p.id === pillId);
            if (!pill || !playerData.inventory[pillId]) return;
            
            playerData.inventory[pillId]--;
            
            // 搴旂敤鏁堟灉
            if (pill.cultivationBonus) {
                playerData.cultivation += pill.cultivationBonus;
                createFloatingText(`+${pill.cultivationBonus} 淇负`, 'gold');
            }
            
            if (pill.tempComprehension) {
                playerData.tempBuffs.comprehension += pill.tempComprehension;
                setTimeout(() => {
                    playerData.tempBuffs.comprehension -= pill.tempComprehension;
                    updateUI();
                }, pill.duration * 1000);
                showMessage(`鎮熸€т复鏃舵彁鍗?{pill.tempComprehension}锛屾寔缁?{Math.floor(pill.duration / 60)}鍒嗛挓`, 'success');
            }
            
            if (pill.permanentTalent) {
                playerData.talent += pill.permanentTalent;
                showMessage(`璧勮川姘镐箙鎻愬崌${pill.permanentTalent}锛乣, 'success');
                createFloatingText('璧勮川鎻愬崌锛?, 'green');
            }
            
            if (pill.breakthroughBonus) {
                playerData.tempBuffs.breakthrough += pill.breakthroughBonus;
                showMessage(`涓嬩竴娆＄獊鐮存垚鍔熺巼+${pill.breakthroughBonus}%`, 'success');
            }
            
            updateUI();
            renderPills();
            saveGame();
        }

        // 鍟嗗簵绯荤粺
        function renderShop() {
            const container = document.getElementById('shopList');
            
            document.getElementById('shopSpiritStones').textContent = formatNumber(playerData.spiritStones);
            
            container.innerHTML = SHOP_ITEMS.map(item => {
                return `
                    <div class="shop-item" onclick="buyShopItem(${item.id})">
                        <div class="shop-item-header">
                            <span class="shop-item-name">${item.name}</span>
                            <span class="shop-item-price">${item.price} 鐏电煶</span>
                        </div>
                        <div class="shop-item-desc">${item.desc}</div>
                    </div>
                `;
            }).join('');
        }

        function buyShopItem(itemId) {
            const item = SHOP_ITEMS.find(i => i.id === itemId);
            if (!item) return;
            
            if (playerData.spiritStones < item.price) {
                showMessage('鐏电煶涓嶈冻锛?, 'fail');
                return;
            }
            
            playerData.spiritStones -= item.price;
            
            if (item.type === 'pill') {
                playerData.inventory[item.itemId] = (playerData.inventory[item.itemId] || 0) + 1;
                showMessage(`璐拱鎴愬姛锛乣, 'success');
            } else if (item.type === 'skill') {
                if (!playerData.ownedSkills.includes(item.itemId)) {
                    playerData.ownedSkills.push(item.itemId);
                    showMessage(`涔犲緱鏂板姛娉曪紒`, 'success');
                } else {
                    showMessage('宸叉嫢鏈夎鍔熸硶锛岃喘涔板け璐ラ€€娆?, 'fail');
                    playerData.spiritStones += item.price;
                }
            }
            
            updateUI();
            renderShop();
            saveGame();
        }

        // 杈呭姪鍑芥暟
        function checkBreakthroughAvailability() {
            const nextRealm = REALMS[playerData.realm];
            const btn = document.getElementById('breakthroughBtn');
            
            if (nextRealm && playerData.cultivation >= nextRealm.requiredCultivation) {
                btn.disabled = false;
            } else {
                btn.disabled = true;
            }
        }

        function checkTempBuffs() {
            // 涓存椂buff鐢眘etTimeout鑷姩娓呴櫎
        }

        

            playerData.autoCultivate = !playerData.autoCultivate;
            document.getElementById('autoToggle').classList.toggle('active', playerData.autoCultivate);
        }

        function updateUI() {
            document.getElementById('realmBadge').textContent = REALMS[playerData.realm - 1].name;
            document.getElementById('cultivationValue').textContent = formatNumber(playerData.cultivation);
            document.getElementById('spiritValue').textContent = `${Math.floor(playerData.spirit)}/${playerData.maxSpirit}`;
            document.getElementById('talentValue').textContent = playerData.talent.toFixed(2);
            document.getElementById('comprehensionValue').textContent = getTotalComprehension().toFixed(2);

            const nextRealm = REALMS[playerData.realm];
            if (nextRealm) {
                const progress = (playerData.cultivation / nextRealm.requiredCultivation) * 100;
                document.getElementById('progressFill').style.width = `${Math.min(100, progress)}%`;
                document.getElementById('progressPercent').textContent = `${Math.min(100, progress).toFixed(1)}%`;
                document.getElementById('progressText').textContent = 
                    `${formatNumber(playerData.cultivation)} / ${formatNumber(nextRealm.requiredCultivation)}`;
            } else {
                document.getElementById('progressFill').style.width = '100%';
                document.getElementById('progressPercent').textContent = '宸叉弧绾?;
                document.getElementById('progressText').textContent = '宸茶揪鏈€楂樺鐣?;
            }

            document.getElementById('cultivationSpeed').textContent = `${getCultivationGain()}/绉抈;
            document.getElementById('spiritRecovery').textContent = `${getSpiritRecovery()}/绉抈;
            document.getElementById('spiritStones').textContent = formatNumber(playerData.spiritStones);
            document.getElementById('daoHeart').textContent = `+${playerData.failedBreakthroughs * 5}%`;

            checkBreakthroughAvailability();
        }

        function calculateOfflineReward() {
            const now = Date.now();
            const offlineTime = Math.min(24 * 60 * 60 * 1000, now - playerData.lastOnlineTime);
            const offlineSeconds = Math.floor(offlineTime / 1000);

            if (offlineSeconds > 60) {
                const offlineGain = Math.floor(getCultivationGain() * offlineSeconds * 0.5);
                playerData.cultivation += offlineGain;
                
                showMessage(`绂荤嚎${formatTime(offlineSeconds)}锛岃幏寰?{formatNumber(offlineGain)}淇负`, 'success');
            }

            playerData.lastOnlineTime = now;
        }

        function showMessage(text, type = '') {
            const box = document.getElementById('messageBox');
            box.textContent = text;
            box.className = 'message ' + type;
        }

        function createFloatingText(text, color = 'gold') {
            const el = document.createElement('div');
            el.className = 'floating-text';
            el.textContent = text;
            el.style.color = color === 'gold' ? '#FFD700' : 
                            color === 'purple' ? '#9B59B6' : 
                            color === 'green' ? '#2ECC71' : '#E74C3C';
            el.style.left = Math.random() * 300 + 100 + 'px';
            el.style.top = '300px';
            document.body.appendChild(el);

            setTimeout(() => el.remove(), 1000);
        }

        function formatNumber(num) {
            if (num >= 10000) {
                return (num / 10000).toFixed(1) + '涓?;
            }
            return num.toLocaleString();
        }

        function formatTime(seconds) {
            const h = Math.floor(seconds / 3600);
            const m = Math.floor((seconds % 3600) / 60);
            if (h > 0) return `${h}灏忔椂${m}鍒嗛挓`;
            return `${m}鍒嗛挓`;
        }

        function saveGame() {
            playerData.lastOnlineTime = Date.now();
            localStorage.setItem('cultivationGame', JSON.stringify(playerData));
        }

        function loadGame() {
            const saved = localStorage.getItem('cultivationGame');
            if (saved) {
                const loaded = JSON.parse(saved);
                playerData = { ...playerData, ...loaded };
                
                // 鐗堟湰鍏煎
                if (!playerData.inventory) playerData.inventory = {};
                if (!playerData.tempBuffs) playerData.tempBuffs = { comprehension: 0, breakthrough: 0 };
            }
        }

        // 鍚姩娓告垙
        init();
    </script>
</body>
</html>

